experiment_name: 'sentiment140'
seed: 42
device: "cuda"
output_dir: "results/nlp/"

data_params:
  dataset_name: 'sentiment140'
  root: 'data/sentiment140'
  download: true

fl_params:
  num_clients: 1000 
  clients_per_round: 20 
  num_rounds: 200   

training_params:
  batch_size: 20
  local_epochs: 1  
  optimizer: 'adam'
  lr: 0.001
  momentum: 0.0 
  weight_decay: 0.0001

attack_params:
  enabled: false

defense_params:
  enabled: false